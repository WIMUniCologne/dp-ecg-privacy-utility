# Pinned to the versions the reported results were produced with.
# Python 3.12. A GPU is not required but the sweeps are slow without one.
tensorflow==2.18.0
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
pandas==2.2.3
matplotlib==3.9.4
seaborn==0.13.2
wfdb==4.3.0
diffprivlib==0.6.5
imbalanced-learn==0.12.4
joblib==1.4.2
notebook==7.5.6
pytest==8.3.4  # tests/ only
